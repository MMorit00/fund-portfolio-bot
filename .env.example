# Copy this file to `.env` and adjust values for your environment.
# Do NOT put real secrets in this file; commit-safe defaults only.

# SQLite database file path
DB_PATH=data/portfolio.db

# Enable SQL trace logs (1 to enable, 0 to disable)
ENABLE_SQL_DEBUG=0

# NAV data source identifier (placeholder for future external providers)
NAV_DATA_SOURCE=eastmoney

# Discord Webhook for daily report (optional; required only when running daily_report job)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/xxxxx

# Dev seed controls (optional)
# SEED_RESET=1
# SEED_SIMULATE_CONFIRM=1

# ==================== LLM Configuration (AI Module) ====================

# API endpoint URL (OpenAI Compatible protocol)
# Default: Zhipu GLM-4 API
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/

# API key for the LLM provider (REQUIRED)
# Get your key from: https://open.bigmodel.cn/usercenter/apikeys
LLM_API_KEY=your_api_key_here

# Model name to use
# Default: glm-4-flash (free/low-cost model)
# Alternatives: glm-4-plus, glm-4-air (Zhipu)
#              qwen-max, qwen-plus (Alibaba)
#              deepseek-chat (DeepSeek)
LLM_MODEL=glm-4-flash

# Maximum retry attempts for API calls (default: 3)
LLM_MAX_RETRIES=3

# Request timeout in seconds (default: 30)
LLM_TIMEOUT=30

# Enable debug logging for AI module (true/false, default: false)
LLM_DEBUG=false
